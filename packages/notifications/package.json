{
  "name": "@pospe/notifications",
  "private": true,
  "version": "0.1.0",
  "description": "Shared notification templates/senders (email, SMS, push, WhatsApp)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext ts,tsx",
    "clean": "rm -rf dist node_modules .turbo"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
