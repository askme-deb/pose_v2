{
  "name": "@pospe/api-client",
  "private": true,
  "version": "0.1.0",
  "description": "Typed fetch client shared by web/mobile/desktop apps",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext ts,tsx",
    "clean": "rm -rf dist node_modules .turbo"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
