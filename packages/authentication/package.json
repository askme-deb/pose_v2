{
  "name": "@pospe/authentication",
  "private": true,
  "version": "0.1.0",
  "description": "Shared auth helpers: token verification, session types",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "eslint . --ext ts,tsx",
    "clean": "rm -rf dist node_modules .turbo"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
