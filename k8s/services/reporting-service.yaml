apiVersion: apps/v1
kind: Deployment
metadata:
  name: reporting-service
  namespace: pospe
  labels:
    app: reporting-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reporting-service
  template:
    metadata:
      labels:
        app: reporting-service
    spec:
      containers:
        - name: reporting-service
          image: pospe/reporting-service:latest
          ports:
            - containerPort: 4009
          envFrom:
            - configMapRef:
                name: pospe-config
            - secretRef:
                name: pospe-secrets
          env:
            - name: PORT
              value: "4009"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 4009
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4009
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: pospe
spec:
  selector:
    app: reporting-service
  ports:
    - port: 4009
      targetPort: 4009
  type: ClusterIP
