apiVersion: apps/v1
kind: Deployment
metadata:
  name: synchronization-service
  namespace: pospe
  labels:
    app: synchronization-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: synchronization-service
  template:
    metadata:
      labels:
        app: synchronization-service
    spec:
      containers:
        - name: synchronization-service
          image: pospe/synchronization-service:latest
          ports:
            - containerPort: 4010
          envFrom:
            - configMapRef:
                name: pospe-config
            - secretRef:
                name: pospe-secrets
          env:
            - name: PORT
              value: "4010"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: synchronization-service
  namespace: pospe
spec:
  selector:
    app: synchronization-service
  ports:
    - port: 4010
      targetPort: 4010
  type: ClusterIP
